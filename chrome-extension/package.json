{
  "name": "revela-chrome-extension",
  "version": "1.0.0",
  "description": "AI-powered Chrome extension for analyzing charts and tables",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r public/* dist/ && cp -r src dist/",
    "lint": "eslint src/**/*.js"
  },
  "keywords": [
    "chrome-extension",
    "ai",
    "chart-analysis",
    "gemma"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
